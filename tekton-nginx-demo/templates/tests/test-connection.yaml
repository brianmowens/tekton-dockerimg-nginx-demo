apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tekton-nginx-demo.fullname" . }}-test-connection"
  labels:
    {{- include "tekton-nginx-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tekton-nginx-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
