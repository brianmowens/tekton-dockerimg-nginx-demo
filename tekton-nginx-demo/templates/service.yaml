apiVersion: v1
kind: Service
metadata:
  name: {{ include "tekton-nginx-demo.fullname" . }}
  labels:
    {{- include "tekton-nginx-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tekton-nginx-demo.selectorLabels" . | nindent 4 }}
